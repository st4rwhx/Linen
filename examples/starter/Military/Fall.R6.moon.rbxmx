<?xml version='1.0' encoding='utf-8'?>
<roblox xmlns:xmime="http://www.w3.org/2005/05/xmlmime" xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance" xsi:noNamespaceSchemaLocation="http://www.roblox.com/roblox.xsd" version="4"><Item class="Folder" referent="RBX0"><Properties><string name="Name">Linen_Militaire_Fall</string></Properties><Item class="ModuleScript" referent="RBX1"><Properties><string name="Name">Motion</string><ProtectedString name="Source">--!strict
-- Generated by Linen. The motion only: no rig, no assumptions about one.
--
-- Each key is a frame and the 12 CFrame components of the transform the
-- animation applies to that part's joint — the same thing a
-- KeyframeSequence Pose carries.
--
-- Named fields rather than {frame, components}: a mixed array infers as
-- the type of its first element, so every key in the file came back as a
-- type error under strict mode.

export type Key = { frame: number, cframe: { number } }
export type Motion = {
	Name: string,
	FPS: number,
	Length: number,
	Looped: boolean,
	Tracks: { [string]: { Key } },
}

local motion: Motion = {
	Name = "Militaire_Fall",
	FPS = 30,
	Length = 27,
	Looped = true,

	Tracks = {
		["Head"] = {
			{ frame = 0, cframe = {0, 0, 0, 0.994522, -0.104528, 0, 0.104274, 0.992099, -0.069756, 0.007292, 0.069374, 0.997564} },
			{ frame = 14, cframe = {0, 0, 0, 0.994377, -0.105894, 0.000047, 0.105649, 0.992049, -0.068387, 0.007195, 0.068007, 0.997659} },
			{ frame = 20, cframe = {0, 0, 0, 0.984808, -0.173648, 0, 0.173648, 0.984808, 0, 0, 0, 1} },
			{ frame = 27, cframe = {0, 0, 0, 0.994522, -0.104528, 0, 0.104274, 0.992099, -0.069756, 0.007292, 0.069374, 0.997564} },
		},
		["Left Arm"] = {
			{ frame = 0, cframe = {0, 0, 0, 0.804412, 0.585525, 0.100413, -0.542582, 0.792951, -0.277187, -0.241922, 0.16849, 0.955555} },
			{ frame = 14, cframe = {0, 0, 0, 0.799724, 0.592397, 0.097505, -0.549063, 0.787365, -0.280334, -0.242841, 0.170653, 0.954938} },
			{ frame = 15, cframe = {0, 0, 0, 0.765653, 0.638678, 0.076587, -0.592809, 0.74681, -0.301416, -0.249704, 0.185379, 0.950412} },
			{ frame = 18, cframe = {0, 0, 0, 0.558127, 0.828506, -0.045511, -0.775144, 0.501039, -0.384853, -0.296051, 0.250074, 0.921855} },
			{ frame = 19, cframe = {0, 0, 0, 0.510917, 0.856574, -0.072413, -0.802811, 0.445334, -0.39645, -0.307341, 0.260687, 0.915196} },
			{ frame = 20, cframe = {0, 0, 0, 0.503983, 0.860333, -0.076343, -0.806542, 0.437157, -0.397975, -0.309017, 0.262147, 0.914214} },
			{ frame = 21, cframe = {0, 0, 0, 0.503983, 0.860333, -0.076343, -0.806542, 0.437157, -0.397975, -0.309017, 0.262147, 0.914214} },
			{ frame = 27, cframe = {0, 0, 0, 0.804412, 0.585525, 0.100413, -0.542582, 0.792951, -0.277187, -0.241922, 0.16849, 0.955555} },
		},
		["Left Leg"] = {
			{ frame = 0, cframe = {0, 0, 0, 0.898794, 0.43597, 0.045822, -0.438371, 0.89387, 0.09395, 0, -0.104528, 0.994522} },
			{ frame = 14, cframe = {0, 0, 0, 0.902967, 0.427348, 0.044987, -0.429709, 0.897985, 0.094724, 0.000082, -0.104864, 0.994487} },
			{ frame = 15, cframe = {0, 0, 0, 0.929615, 0.366463, 0.038989, -0.368531, 0.924235, 0.099869, 0.000564, -0.107209, 0.994236} },
			{ frame = 16, cframe = {0, 0, 0, 0.990432, 0.134723, 0.029897, -0.137273, 0.984028, 0.113338, -0.01415, -0.116358, 0.993107} },
			{ frame = 17, cframe = {0, 0, 0, 0.999636, 0.021392, 0.016438, -0.023183, 0.99276, 0.117853, -0.013798, -0.118191, 0.992895} },
			{ frame = 20, cframe = {0, 0, 0, 0.99852, 0.054217, -0.004239, -0.053282, 0.990941, 0.123277, 0.010884, -0.122869, 0.992363} },
			{ frame = 23, cframe = {0, 0, 0, 0.999818, 0.012905, 0.01408, -0.014478, 0.9929, 0.118071, -0.012457, -0.118254, 0.992905} },
			{ frame = 24, cframe = {0, 0, 0, 0.994578, 0.101399, 0.023094, -0.103393, 0.987998, 0.114759, -0.01118, -0.116525, 0.993125} },
			{ frame = 25, cframe = {0, 0, 0, 0.957989, 0.285155, 0.030724, -0.286804, 0.952115, 0.105925, 0.000952, -0.110287, 0.993899} },
			{ frame = 27, cframe = {0, 0, 0, 0.898794, 0.43597, 0.045822, -0.438371, 0.89387, 0.09395, 0, -0.104528, 0.994522} },
		},
		["Right Arm"] = {
			{ frame = 0, cframe = {0, 0, 0, 0.968628, -0.181114, -0.170167, 0.205888, 0.968312, 0.141357, 0.139173, -0.171958, 0.975224} },
			{ frame = 14, cframe = {0, 0, 0, 0.967418, -0.184759, -0.173105, 0.210338, 0.967066, 0.143325, 0.140924, -0.175066, 0.974419} },
			{ frame = 15, cframe = {0, 0, 0, 0.958393, -0.209704, -0.193669, 0.241206, 0.95776, 0.156577, 0.152653, -0.196776, 0.968492} },
			{ frame = 18, cframe = {0, 0, 0, 0.89677, -0.324655, -0.30067, 0.394722, 0.89402, 0.211951, 0.199994, -0.308752, 0.929879} },
			{ frame = 20, cframe = {0, 0, 0, 0.879153, -0.348021, -0.325532, 0.428792, 0.875763, 0.22176, 0.207912, -0.334546, 0.919158} },
			{ frame = 21, cframe = {0, 0, 0, 0.879153, -0.348021, -0.325532, 0.428792, 0.875763, 0.22176, 0.207912, -0.334546, 0.919158} },
			{ frame = 27, cframe = {0, 0, 0, 0.968628, -0.181114, -0.170167, 0.205888, 0.968312, 0.141357, 0.139173, -0.171958, 0.975224} },
		},
		["Right Leg"] = {
			{ frame = 0, cframe = {0, 0, 0, 0.996176, 0.086741, 0.010471, -0.087353, 0.991223, 0.099229, -0.001772, -0.099764, 0.99501} },
			{ frame = 14, cframe = {0, 0, 0, 0.993609, 0.112424, 0.010114, -0.112873, 0.988728, 0.098371, 0.001059, -0.098884, 0.995098} },
			{ frame = 18, cframe = {0, 0, 0, 0.999923, -0.012391, 0.000347, 0.012316, 0.996253, 0.085609, -0.001406, -0.085598, 0.996329} },
			{ frame = 20, cframe = {0, 0, 0, 0.99944, -0.033418, -0.001541, 0.033431, 0.996035, 0.082445, -0.00122, -0.08245, 0.996594} },
			{ frame = 21, cframe = {0, 0, 0, 0.999507, -0.031352, -0.00153, 0.031371, 0.996094, 0.08254, -0.001063, -0.082547, 0.996587} },
			{ frame = 27, cframe = {0, 0, 0, 0.989758, 0.142395, 0.010181, -0.142704, 0.984903, 0.097984, 0.003925, -0.098433, 0.995136} },
		},
		["Torso"] = {
			{ frame = 0, cframe = {0, 0, 0, 0.994522, 0.104528, 0, -0.10294, 0.979413, 0.173648, 0.018151, -0.172697, 0.984808} },
			{ frame = 14, cframe = {0, 0, 0, 0.994378, 0.105892, 0.000094, -0.10435, 0.97974, 0.170943, 0.018009, -0.169992, 0.985281} },
			{ frame = 18, cframe = {0, 0, 0, 0.986641, 0.162906, 0.000643, -0.16268, 0.985049, 0.056688, 0.008602, -0.056035, 0.998392} },
			{ frame = 20, cframe = {0, 0, 0, 0.984808, 0.173648, 0, -0.173542, 0.984208, 0.034899, 0.00606, -0.034369, 0.999391} },
			{ frame = 21, cframe = {0, 0, 0, 0.984808, 0.173648, 0, -0.173542, 0.984208, 0.034899, 0.00606, -0.034369, 0.999391} },
			{ frame = 27, cframe = {0, 0, 0, 0.994522, 0.104528, 0, -0.10294, 0.979413, 0.173648, 0.018151, -0.172697, 0.984808} },
		},
	},
}

return motion
</ProtectedString></Properties></Item><Item class="Script" referent="RBX2"><Properties><string name="Name">InstallerLinen</string><ProtectedString name="Source">--!strict
-- Generated by Linen. Run this once, in Studio, with the rig in the Workspace.
--
--   1. Drag this folder into ServerStorage (anywhere works).
--   2. Right-click the script &gt; Run Script. Or paste it in the Command Bar.
--   3. Open Moon Animator &gt; Load &gt; "Militaire_Fall".
--
-- It builds a Moon Animator 2 save out of the motion in the Motion module and
-- the joints of the rig as it actually is. Nothing is uploaded and nothing
-- outside ServerStorage.MoonAnimator2Saves is touched.

local ServerStorage = game:GetService("ServerStorage")
local HttpService = game:GetService("HttpService")

-- `script.Parent` is `Instance?` to the type checker, and this script is only
-- ever run from inside the folder Linen wrote, so say so rather than index
-- through a maybe-nil.
local folder = assert(script.Parent, "Linen: lance le script depuis le dossier Linen_*")
local Motion = require(folder:WaitForChild("Motion"))

local RIG_NAME = "Dummy"

local function findRig(): Model
	local named = workspace:FindFirstChild(RIG_NAME)

	if named and named:IsA("Model") and named:FindFirstChildWhichIsA("Humanoid") then
		return named
	end

	for _, model in workspace:GetChildren() do
		if model:IsA("Model") and model:FindFirstChildWhichIsA("Humanoid") then
			return model
		end
	end

	error(
		"Linen: aucun rig dans le Workspace. Construis-en un (Avatar &gt; Rig Builder "
		.. "&gt; R15) puis relance le script."
	)
end

-- Every Motor6D, keyed by the name of the part it drives. This is the same
-- key Moon's own reader uses, which is why the hierarchy below is written in
-- part names rather than joint names.
local function motorsByPart(rig: Model): { [string]: Motor6D }
	local motors: { [string]: Motor6D } = {}

	for _, descendant in rig:GetDescendants() do
		if descendant:IsA("Motor6D") and descendant.Part1 then
			motors[(descendant.Part1 :: BasePart).Name] = descendant
		end
	end

	return motors
end

-- "LowerTorso.UpperTorso.RightUpperArm": the chain of driven parts down to
-- this one. It stops at the highest part that is itself driven by a joint,
-- because that is where Moon's reader starts walking.
local function hierarchy(motors: { [string]: Motor6D? }, part: string): string
	local chain = { part }
	local motor = motors[part]

	while motor and motor.Part0 do
		local parent = (motor.Part0 :: BasePart).Name

		if motors[parent] == nil then
			break
		end

		table.insert(chain, 1, parent)
		motor = motors[parent]
	end

	return table.concat(chain, ".")
end

local function pathOf(instance: Instance): ({ string }, { string })
	local names, classes = {}, {}
	local current: Instance? = instance

	while current do
		table.insert(names, 1, current.Name)
		table.insert(classes, 1, current.ClassName)
		current = current.Parent
	end

	return names, classes
end

local function value(class: string, name: string, parent: Instance): any
	local instance = Instance.new(class)
	instance.Name = name
	instance.Parent = parent
	return instance
end

local function folder(name: string, parent: Instance): Folder
	local instance = Instance.new("Folder")
	instance.Name = name
	instance.Parent = parent
	return instance
end

local function toCFrame(components: { any }): CFrame
	return CFrame.new(
		components[1], components[2], components[3],
		components[4], components[5], components[6],
		components[7], components[8], components[9],
		components[10], components[11], components[12]
	)
end

local rig = findRig()
local motors = motorsByPart(rig)

local saves = ServerStorage:FindFirstChild("MoonAnimator2Saves")

if not saves then
	saves = folder("MoonAnimator2Saves", ServerStorage)
end

assert(saves)

local existing = saves:FindFirstChild(Motion.Name)

if existing then
	existing:Destroy()
end

local save = Instance.new("StringValue")
save.Name = Motion.Name

local names, classes = pathOf(rig)

save.Value = HttpService:JSONEncode({
	Information = {
		Length = Motion.Length,
		Looped = Motion.Looped,
		FPS = Motion.FPS,
	},

	Items = {
		{
			Path = {
				ItemType = "Rig",
				InstanceNames = names,
				InstanceTypes = classes,
			},
		},
	},
})

-- "1" is the index of the single item declared above.
local item = folder("1", save)
local rigFolder = folder("Rig", item)

local written, skipped = 0, {}

for part, keys in Motion.Tracks do
	local motor = motors[part]

	if not motor then
		table.insert(skipped, part)
		continue
	end

	local joint = folder("_joint", rigFolder)
	local default = motor.C1

	local hier = value("StringValue", "_hier", joint)
	hier.Value = hierarchy(motors, part)

	local rest = value("CFrameValue", "default", joint)
	rest.Value = default

	local keyframes = folder("_keyframes", joint)

	for _, key in keys do
		local frame = key.frame
		local transform = toCFrame(key.cframe)

		-- Moon stores the joint's animated C1. Its reader recovers the
		-- transform as `c1:Inverse() * default`, so this is that, inverted.
		local pack = folder(tostring(frame), keyframes)
		local values = folder("Values", pack)

		local at = value("CFrameValue", "0", values)
		at.Value = default * transform:Inverse()

		local eases = folder("Eases", pack)
		local ease = folder("0", eases)
		local easeType = value("StringValue", "Type", ease)
		easeType.Value = "Linear"

		local params = folder("Params", ease)
		local direction = value("StringValue", "Direction", params)
		direction.Value = "InOut"
	end

	written += 1
end

save.Parent = saves

print(
	`Linen: "{Motion.Name}" -&gt; ServerStorage.MoonAnimator2Saves ({written} `
	.. `articulations, {Motion.Length} images a {Motion.FPS} fps). `
	.. `Ouvre Moon Animator &gt; Load.`
)

if #skipped &gt; 0 then
	warn(
		`Linen: {#skipped} parties absentes du rig et ignorees : `
		.. table.concat(skipped, ", ")
	)
end
</ProtectedString></Properties></Item></Item></roblox>